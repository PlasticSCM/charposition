<Window x:Class="charposition.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:charposition.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow"
        Width="800"
        Height="450"
        UseLayoutRounding="True"
        mc:Ignorable="d">
    <Grid>
        <controls:CharsView LineChars="{Binding LineChars}" LineCount="{Binding LineCount}" MaxLineLength="{Binding MaximumLineLength}" />
    </Grid>
</Window>
